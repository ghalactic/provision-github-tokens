# yaml-language-server: $schema=https://ghalactic.github.io/provision-github-tokens/schema/requester.v1.schema.json

tokens:
  ci-fixtures:
    as: ci
    account: ghalactic-fixtures
    repos:
      - provision-github-tokens-ci-consumer
    permissions:
      actions: write
      contents: read

provision:
  secrets:
    FIXTURES_GITHUB_TOKEN:
      token: ci-fixtures
      github:
        repo:
          actions: true
